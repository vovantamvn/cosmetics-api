spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: "jdbc:postgresql://localhost:5432/test_cosmetics"
    username: postgres
    password: postgres
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 215MB

resetdata: false